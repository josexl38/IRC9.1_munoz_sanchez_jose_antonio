version: '3'

services:
  flask-ansible:
    build: .
    container_name: flask-ansible-ui
    ports:
      - "5001:5001"
    volumes:
      - .:/app
    environment:
      - ANSIBLE_HOST_KEY_CHECKING=False
